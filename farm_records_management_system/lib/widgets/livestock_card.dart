import 'package:flutter/material.dart';

class LivestockCard extends StatelessWidget {
  const LivestockCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Go Back')),
    );
  }
}

